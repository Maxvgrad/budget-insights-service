version: '3.7'

services:
  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: budget-insights
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
